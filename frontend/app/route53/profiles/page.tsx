import { PlaceholderPage } from "@/components/layout/placeholder-page";

export const metadata = { title: "Profiles" };

export default function ProfilesPage() {
  return (
    <PlaceholderPage
      description="Share DNS configuration across mock environments."
      title="Profiles"
    />
  );
}
